# Qualified DISSOLVE Inherited-Form Manifest v0

Status:

```text
layer: MANIFEST
date: 2026-08-13
source chaos:
  docs/00_chaos/dissolve_network_rejected_generation_target_notes_2026-08-12.md
  docs/00_chaos/dissolve_campaign_conservation_and_resume_notes_2026-08-12.md
source tables:
  docs/01_table/yellowprints/qa_rejected_lineage_recovery_yellowprint.v0.md
  docs/01_table/yellowprints/network_rejected_form_materialization_yellowprint.v0.md
  docs/01_table/yellowprints/qualified_dissolve_inherited_form_yellowprint.v0.md
source crystall:
  docs/02_crystall/blueprints/qa_rejected_lineage_recovery.v0.md
  docs/02_crystall/blueprints/network_rejected_form_materialization.v0.md
  docs/02_crystall/blueprints/qualified_dissolve_inherited_form.v0.md
implementation: runtime_confirmed for the direct inherited-form treatment
authority: explicit router_mode=tree + pressure_policy=qualified_need_v0
default authority change: none
general DISSOLVE promotion: not authorized
semantic-age/every-tick collection: deferred
```

## Result

The first production-grown direct-unit DISSOLVE treatment now executes this
causal chain:

```text
QA-rejected ancestor
-> body-derived completion assessment
-> bounded recovery carrier
-> pure NETWORK continuation projection
-> clean descendant birth
-> FLOW materializes current work + inherited rejected form
-> qualified blocking release witness
-> E02 ▽ -> ☷ committed and executed
-> atomic target release + bounded residue
-> one exact post-release observation need
-> E07 ☷ -> ☴ committed and executed
```

No harness supplies a DISSOLVE reason, target or route. The trusted test host
supplies only a separately verified fresh empty child repository identity. The
body derives every semantic and physical operand from the ancestor corpse,
lineage ledger, carrier, NETWORK projection and current Packet field.

## Truth Boundary

The treatment preserves two different claims:

```text
ancestor QA rejection                         runtime_confirmed
applicability of the failed form to child     inherited_proposal
release act in the child                      runtime_confirmed
continued historical QA evidence              runtime_confirmed
```

DISSOLVE changes only current descendant applicability. It does not erase or
rewrite the ancestor verdict, rejected check refs, candidate seal, source
corpse, task identity or lineage history.

## NETWORK And FLOW

`runtime/network_projection.lua` derives one closed projection from the exact
tuple:

```text
lineage state
source corpse
completion ledger event
selected recovery carrier
```

The projection contains current work and one bounded rejected-form record. It
contains no live Packet, mutable lineage, provider handle, capability, ancestor
field, ancestor CALM or full trace.

`runtime/network_ingress.lua` independently verifies that tuple before birth.
`runtime/packet_birth.lua` accepts the projection only through trusted vertical
Packet life. `organs/flow.lua` then materializes exactly:

```text
network_current_work
inherited_rejected_form
```

The current-work prompt is canonical. The full carrier is not installed as a
third semantic alias. If a rejected ancestor already owned a repository and no
fresh child root is available, `runtime/lineage_runner.lua` suspends with
`fresh_repository_allocation_required`; it never reuses the failed root.

## Qualified Release

The named consumer is:

```text
dissolve.inherited_rejected_form.v0
causal class: blocking_demand
target: ☷
```

Its witness and `pressure.action_plan.v0` bind:

```text
Packet id and generation
target unit id and exact version
NETWORK projection id
carrier id and source corpse id
historical QA id
candidate seal id and verdict id
planned residue unit id
current potential revision
```

`runtime/pressure_composition.lua` resolves the consumer as named provenance.
The route carries the immutable action. `organs/dissolve.lua` readiness then
re-derives the same projection, target, version, reason and refs independently.
Caller replacement of any action-owned field is a loud harness invariant.

## Atomic Effect

`runtime/body.lua` and `runtime/field.lua` implement one protected transaction:

```text
validate exact live inherited form
reserve deterministic residue id
normalize release and residue records
append dedicated unit_dissolution event
change target activation to dissolved and version +1
create one rejected_form_residue unit at version 1
advance potential exactly once
```

The transaction is all-or-nothing. A malformed residue, stale allocation,
foreign source tuple or injected field-commit failure leaves trace, field and
revisions equal to their pre-call snapshots. Generic field activation cannot
release or reactivate this tagged unit. Exact replay returns
`already_released` without a second event or revision.

The event is dedicated and actor-owned; generic `append_trace` cannot forge it.
Dead-Packet finality makes every release API inert.

## Mass And Loss

The effect records two independent measurements:

```text
released_mass.forms = 1
released_mass.relations = 0
irreversible_identity_loss = 0
residue preserved = true
ordinary body-tick cost = 1 step
substrate calls during ☷ = 0
```

Released mass is not identity loss. Zero irreversible loss is lawful only
because the exact historical evidence survives in bounded residue. Omitting
the residue or its source refs while claiming zero loss is rejected.

## Post-Release Observation

Before release, current work is intentionally deferred: the child must not
reason from inherited failed form as if it were current truth.

After release, `runtime/upper_coverage.lua` derives one mixed observation over
exactly three current versions:

```text
network_current_work                 semantic
released inherited_rejected_form    material + semantic consequence
rejected_form_residue               semantic
```

The action carries presentation policy
`network.rejected_form_after_release.v0`. `organs/observe.lua` rejects a
missing policy, a partial three-object join and any caller prompt override
before calling the substrate.

The resulting prompt contains current work exactly once plus the canonical
bounded residue. It does not expose the full inherited rejected form or its
artifact-alignment payload. The observation covers the exact three source
versions and its own response unit.

## Grown Evidence

The positive corpus starts from a real QA-rejected ancestor grown by the QA
body fixture. It passes through completion, carrier, NETWORK, continuation,
birth and FLOW before Tree derives its first route.

The canonical edge observer records:

```text
E02 ▽ -> ☷    committed=1 executed=1
E07 ☷ -> ☴    committed=1 executed=1
unit_dissolution events=1
released target activation=dissolved
residue units=1
```

Matched controls prove causality:

```text
consumer ablated:
  no E02 route, no release event, inherited form remains live

no substrate:
  ☷ still releases successfully with zero substrate calls
  the later semantic boundary becomes typed stalled/no_viable_edge

authority instrument off/on:
  exact life projection equal
  Packet route/state/budget/loss equal
  captured corpse equal
```

The observer therefore measures the effect but does not cause it.

## Named Readers

| Written fact | Named reader | Activation |
|---|---|---|
| terminal QA projection | corpse capture and completion | ancestor terminal |
| corpse QA evidence | completion and carrier builder | lineage evaluation |
| carrier QA history | NETWORK projection | continuation decision |
| NETWORK current work | FLOW and post-release OBSERVE | descendant birth/release |
| inherited rejected form | qualified DISSOLVE consumer | Tree derivation |
| release action in route | registry and DISSOLVE readiness | committed ☷ arrival |
| unit_dissolution event | effect verifier and replay guard | release commit/replay |
| rejected-form residue | upper coverage and OBSERVE policy | post-release derivation |
| changed unit versions | upper coverage | exact version change |
| observer edge records | edge-stats v3/current audits | runner closure |

No new mutable need, age or release ledger was added.

## Verification

```text
lua tests/run.lua                                      136 suites passed
lua tests/smoke_mortality_battery.lua                  8/8 passed
tests/test_network_rejected_projection.lua             passed
tests/test_network_rejected_materialization.lua        passed
tests/test_inherited_form_dissolve.lua                 passed
tests/test_inherited_form_dissolve_hostile.lua         passed
tests/test_inherited_form_dissolve_life.lua            passed
tests/test_dissolve_p10_evidence.lua                   passed
tests/test_edge_masslessness.lua                       6/6 passed
luac -p over changed Lua sources                       passed
git diff --check                                       passed
```

The separate prospective raw-stale gate remains intentionally red:

```text
tests/pending_dissolve_live_route_gate.lua
R3-01 green
R3-02 green
R3-03 green
R3-04 red: ordinary life still has no stale/release writer before ENCODE
```

## Promotion Decision

Accepted now:

```text
direct inherited-rejected-form treatment inside explicit qualified Tree lives
physical E02 and E07 evidence for this treatment
exact release/residue/presentation contracts
```

Not accepted:

```text
default router authority
general P10/full-tree promotion
all DISSOLVE targets or all E07 directions
automatic raw/formed relation garbage collection
support age, every-tick collection or semantic time
the claim that DISSOLVE itself creates later insight
```

## P10 Reader Amendment - 2026-08-13

The initial manifest recorded that the generic P10 evaluator still read the
old crystallization `boundary.loss_records` channel. That reader gap is now
superseded by
[`dissolve_p10_evidence.v0.md`](dissolve_p10_evidence.v0.md).

The treatment-specific P10 case is green through exact release/residue evidence
and executable controls. General DISSOLVE and full-tree promotion remain
blocked; the reader correction grants no new route or default authority.
