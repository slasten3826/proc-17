# DISSOLVE P10 Evidence Manifest v0

```text
layer: MANIFEST
date: 2026-08-13
source chaos:
  docs/00_chaos/dissolve_p10_evidence_reader_notes_2026-08-13.md
source table:
  docs/01_table/yellowprints/dissolve_p10_evidence_yellowprint.v0.md
source crystall:
  docs/02_crystall/blueprints/dissolve_p10_evidence.v0.md
implementation: runtime_confirmed for the direct inherited-form P10 slice
default authority change: none
general DISSOLVE promotion: not authorized
full-tree promotion: not authorized
```

## Result

The P10 reader no longer treats an arbitrary crystallization loss plus any
direction mentioning `☷` as proof of DISSOLVE. The direct inherited-form slice
now becomes green only through this immutable join:

```text
credited canonical arrival into ☷
+ exactly one normalized unit_dissolution event
+ exact dissolved target version and activation
+ exact rejected-form residue and ancestor provenance
+ semantically matched no-rigidity control
+ massless observer mirror
```

The release records `released_mass.forms=1` and zero irreversible identity
loss. This is lawful because the historical QA evidence survives in bounded
residue. `boundary.loss_records` is neither required nor accepted as a
substitute.

## Grown Corpus

`tests/test_dissolve_p10_evidence.lua` grows one real QA-rejected ancestor and
then derives three deterministic lives:

```text
primary       inherited form present, observer enabled, ▽->☷ executes
mirror        same positive physics, observer disabled, ▽->☷ executes
no_rigidity   same prompt/work mode/physics, no inherited form, no ☷ arrival
```

The observer pair is green. The P10 case record is green under:

```text
evaluator_id      tree-authority.case.P10.unit_dissolution.v1
evaluator_version edge-case-evaluator.p10.release.v1
```

The current report remains `promotion_authorized=false`. This diagnostic
corpus proves one direct treatment; it does not close the other cases or the
38-direction campaign.

## False-Green Closure

Detached hostile projections prove that P10 remains red when any one of these
conditions is substituted for the real join:

```text
old unrelated loss plus a claimed direction, without a release event
release event without eligible credited arrival into ☷
released target restored to live
release residue removed
positive release life mislabeled as the no-rigidity control
```

The dedicated event writer accepts `cost={}`. Packet trace storage
canonicalizes that input to the complete six-axis zero vector, and the reader
verifies the stored form.

## Diagnostic Correction

The first grown corpus exposed one classification error:

```text
network_current_work_deferred_by_inherited_form
```

That state is the selected treatment order, not unqualified pressure. Current
work remains physically deferred while the inherited form is live, but no
writerless diagnostic is emitted merely to make the global corpus reject the
route. Consumer ablation and every real incomplete/unsupported diagnostic
remain promotion-ineligible.

## Verification

```text
lua tests/test_dissolve_p10_evidence.lua        passed
lua tests/test_edge_case_manifest.lua           passed
lua tests/test_edge_corpus.lua                  passed
lua tests/test_edge_current_report.lua          passed
lua tests/test_qualified_pressure_shadow.lua    passed
lua tests/test_inherited_form_dissolve_life.lua passed
lua tests/run.lua                               136 suites passed
lua tests/smoke_mortality_battery.lua           8/8 passed
luac -p changed P10 Lua sources                 passed
git diff --check                                passed
```

The prospective raw-stale relation gate remains deliberately outside the
green suite:

```text
tests/pending_dissolve_live_route_gate.lua
R3-01 green
R3-02 green
R3-03 green
R3-04 red: no ordinary stale/release writer exists before ENCODE
```

## Boundary

Accepted:

```text
direct inherited rejected-form P10 evidence
exact release/residue reader
executable no-rigidity control
treatment-specific evaluator identity
```

Deferred:

```text
raw-stale and formed-relation DISSOLVE
semantic age and recurring collection
multiple releases in one Packet life
non-zero irreversible DISSOLVE loss
P12 family closure and 38-direction closure
default Tree authority
```
