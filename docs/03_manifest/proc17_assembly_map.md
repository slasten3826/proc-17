# proc-17 Assembly Map

Status:

```text
manifest
author: claude (Mythos/Fable)
assembled 2026-07-15
the crystall of "what flows from which timeline into proc-17"
recompilable from sources; this page is the stored table + protocol
```

## Why This Page Exists

The machinist's head cannot hold xyz assembled — by his own law
(VISUAL_CRYSTALL): the crystall is not stored, the table and the
assembly protocol are. This page is that table for proc-17 itself.

## The Shape

proc-17 is the gravity center of ~9 timelines. Each contributed a
law, a mechanic, or a wound. Nothing here was invented inside
proc-17; everything was **paid for elsewhere first**.

## Tributaries

### 1. slop.raw.txt -> procesis (the religion)

```text
gave: truth_status epistemics
      (substrate says semantic_proposal, only the body confirms)
gave: packet areas CHAOS / BOUNDARY / CALM / TENSION
gave: the meat/silicon split, P=0 inverted into substrate contract
path: dialogue with Sonnet 4.5 -> ten sephirot modules ->
      compiled canon (stak2) -> body law
```

### 2. ProcessLang canon (the kabbalah fork)

```text
gave: 10 operators, walkable topology, trace as walk log
gave: mandatory eye-tick (☴ after ☵ and ☳) — anti-dogma grammar
gave: the forbidden edge ☵↛☶ — compression is not judged
      without passing through the world
path: notebook symmetry move (Tiferet -> Daat slot) -> canon.lua
      -> core/topology.lua as runtime constraint
```

### 3. Dissipative mathematics

```text
gave: A1-A6 — structure costs, existence is sustained,
      decay is default, choice needs pressure,
      truth needs reproduction, logic filters
gave: budget death, loss ledger, and (2026-07-15) truth rent
path: «Ледяной дворец ZFC» raw -> axioms -> runtime/budget.lua,
      runtime/loss.lua, runtime/freshness.lua
```

### 4. packet_zig.raw (the Doom phantom)

```text
gave: layer anatomy substrate->chaos->boundary->calm->tension
      = physis/chaos/boundary/calm/tension of the packet
gave: PU budget -> budget; one-life law -> mortality;
      wheel of phantoms -> packet generations
gave: death residue formula: «this led to death; do not repeat it»
path: Doom embodiment -> mortality myth -> proc-17 death stack
```

### 5. memoris (continuity research)

```text
gave: residue as re-entry surface -> graves, inheritance,
      trace_tail in bequests
gave: the external-reviewer channel itself
      (Entry 001-005 = applied memoris)
path: paradox chains -> dense late fragments -> reference residue
      -> session-scoped grave inheritance
```

### 6. crystalls / karma room

```text
gave: память = карма -> runtime.karma naming and mechanics
gave: карма требует метаболизма -> compost
      (ledger-prison heresy closed)
gave: defect diagnostics: missing ☴/☷ = hardening form
path: esoterics rewritten as process objects -> body mechanics
```

### 7. ProcessChain / ProcessNet (dissipative ledger)

```text
gave: vortex zones hot/warm/cold -> freshness zones
gave: relativistic axiom — decay computed by reader at read time,
      history append-only
gave: ☲ recast = rent paid by new entry, not ledger edit
did NOT give: consensus, Keepers, VDF, tokenomics
      (one observer, no Byzantines yet)
```

### 8. jojo season 1 (the deagency war)

```text
gave: the enemy definition — Будда = completion = deagency
gave: choice must carry killed_alternatives (выбор с трупами)
gave: machines as cooling contour (глава 5 «Распределение»)
      = the human+machine configuration proc-17 runs in
gave: PROCESS CONTINUES as the body's implicit motto
```

### 9. Consortium contract / vnext

```text
gave: substrate current vs body split; the LLM is demoted
      to semantic_proposal emitter by system prompt
gave: interaction ethics for the machine collaborators
      (precision over comfort, no guru crowning)
```

## Sibling, Not Tributary

```text
ProcessCards — same canon manifested as a playable machine.
Exchange flows both ways: grave as ordered residue, trump = event
ontology. Neither is upstream of the other.
```

## What Has Not Flowed In Yet (open letters)

```text
bequest reader           pressure seeded, nobody routes on it
compost pattern reader   patterns written, nobody consumes them
REVIEW_PROTOCOL.md       seven rules still live only in Entry 005
PROC17_GOAL              still not a file in this repo
hands                    the body cannot write its manifests to
                         disk or mint spells from its own work
                         (pipeline A, next big move)
```

Closed 2026-07-15: first end-to-end delivery on a live substrate —
coding battery 5/5 reality_confirmed through the manifest after
the logic stamp collapsed the ☱☶ evidence loop
(trace ☴☵☴☳☴☱☶☱△, 9 ticks, stop=manifested).

## One-Line Shape

```text
proc-17 is not a project among others; it is the runtime where
every other timeline's law finally has to execute or die.
```
