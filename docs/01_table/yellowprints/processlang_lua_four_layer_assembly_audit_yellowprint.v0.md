# ProcessLang Lua Four-Layer Assembly Audit Yellowprint v0

Status:

```text
table / whole-machine audit
date: 2026-07-18
scope:
  packet-slop museum -> proc-17 living implementation
  L1 CHAOS -> L2 TABLE -> L3 CRYSTALL -> L4 MANIFEST
sources:
  docs/00_chaos/l1_flow_marks_and_l2_relation_lifecycle_notes_2026-07-18.md
  docs/00_chaos/l1_true_chaos_calm_body_notes_2026-07-18.md
  docs/00_chaos/full_packet_tree_physics_notes.md
  docs/01_table/yellowprints/operator_tree_physics_yellowprint.v0.md
  docs/01_table/yellowprints/l1_body_boundary_yellowprint.v0.md
  docs/01_table/yellowprints/relation_consumer_causality_yellowprint.v0.md
  docs/03_manifest/current_state.md
  /home/slasten/work/packet-slop/README.md
  /home/slasten/work/packet-slop/docs/59_RESEARCH_RETROSPECTIVE_RU.md
  /home/slasten/work/packet-slop/docs/60_PROCESSLANG_LUA_MACHINE_BLUEPRINT_RU.md
cold local verification:
  lua tests/run.lua                       45/45 suites green
  lua tests/smoke_mortality_battery.lua   8/8 green
  lua tests/smoke_l1_baselines.lua        completed
production code change authorized: no
crystallization authorized: no
router promotion authorized: no
external review required: no
```

## 0. Audit Verdict

The strongest statement supported today is:

```text
packet-slop preserved the bottom-up search and ended by requesting a clean
ProcessLang Lua machine.

proc-17 independently built a top-down ProcessLang Lua body and has now ported
the museum's canonical L1(C) law with exact parity.

The repositories can therefore be treated as consecutive stages of one
machine-building process, but the vertical L1 -> L2 -> L3 -> L4 causal chain
has not yet been executed end to end.
```

This audit accepts the direction and rejects premature completion.

```text
accepted: proc-17 is the current successor candidate to packet-slop
rejected: copy old stands wholesale into proc-17
rejected: green unit suites prove all four layers are already one machine
rejected: matching glyph names alone prove matching layer physics
```

## 1. Evidence Vocabulary

Every row in this table uses one of these statuses.

| Status | Meaning |
|---|---|
| `GREEN_LOCAL` | Executable contract is confirmed by current code and tests in its own boundary. |
| `GREEN_PARITY` | New code reproduces a named museum oracle exactly. |
| `YELLOW_PARTIAL` | Real implementation exists, but a named reader, writer, transition, or integration life is missing. |
| `RED_MISSING` | Required causal surface does not exist in current production code. |
| `OPEN_LAW` | The architecture has not yet selected one physical law. |
| `MUSEUM_EVIDENCE` | Old stand/result is useful evidence but not current canonical code. |
| `DOCUMENT_DECISION` | Humans/machines selected a direction; runtime has not confirmed it. |

`GREEN_LOCAL` must never be silently promoted to whole-layer or whole-machine
green.

## 2. Source Precedence

When sources disagree, use this order:

| Priority | Source | Authority |
|---:|---|---|
| 1 | Current proc-17 code plus grown integration tests | What the living body actually does |
| 2 | Current proc-17 table/crystall contracts | Intended next body law |
| 3 | packet-slop README plus documents 59/60 | Museum's final architectural essence |
| 4 | packet-slop reproducible stands | Empirical/formula evidence |
| 5 | Older packet-slop hypotheses | Archaeology; may inspire, never silently govern |
| 6 | Model commentary or analogy | Proposal requiring conversion into a falsifiable contract |

This order does not make current code automatically correct. It makes defects
observable as differences between manifested behavior and selected law.

## 3. Four-Layer Map

The packet-slop final blueprint says:

```text
L1 CHAOS    = FLOW + CONNECT + DISSOLVE
L2 TABLE    = CONNECT + DISSOLVE + ENCODE + OBSERVE + CHOOSE
L3 CRYSTALL = ENCODE + CHOOSE + LOGIC + CYCLE + RUNTIME
L4 MANIFEST = MANIFEST
```

Current audit:

| Layer | Museum result | proc-17 result | Current status | Missing proof |
|---|---|---|---|---|
| `L1 CHAOS` | Canonical variant C, ring/core/trace/phase/carry and snapshot metrics | Exact standalone Lua port in `l1/field.lua` | `GREEN_PARITY` standalone | Production source, lifetime, birth mark, body insertion |
| `L2 TABLE` | Rough but working form-production experiments; final five-glyph formula | Units, raw relations, observation, encoding, choice, dissolution APIs | `YELLOW_PARTIAL` | Living L1 projection and four-road relation consumption |
| `L3 CRYSTALL` | Bayesian/self-theorizing form runtime is selected but only partly explored | Working code-task ENCODE/CHOOSE/LOGIC/CYCLE/RUNTIME mechanics | `YELLOW_PARTIAL` | Decide whether generic Bayesian/theory law is required and prove form-runtime continuity |
| `L4 MANIFEST` | Museum deliberately left the body open | proc-17 assembles typed output, terminal, death, and residue | `GREEN_LOCAL` v0 | Whole-machine input provenance and autonomous outward action |

The map is not:

```text
packet-slop owns L1/L2, proc-17 owns L3/L4, concatenate directories
```

It is:

```text
packet-slop found laws from below
proc-17 found executable contracts from above
their overlap must be reconciled at repeated glyphs and layer seams
```

## 4. L1 Audit: CHAOS

### 4.1 Museum law

The final museum record identifies these stable parts:

```text
crazy(a, d)
MOD = 59049
variant = C
ring / core / L1-local trace / phase / carry / position
irreversible local replacement through ticks
bounded fingerprint/density/distinct metrics
```

The source adapter remains explicitly open in the retrospective.

### 4.2 proc-17 implementation

`l1/field.lua` implements:

```text
initialize
tick
snapshot
freeze
```

Confirmed evidence:

```text
full museum fixture exact at tick 1, 7965, and 15930
small-source full trajectory exact
snapshot purity
freeze guard
Lua 5.4 contract
bounded source size
```

The pre-registered baseline does not show generic superiority over a seeded
PRNG. It shows deterministic local propagation: a final-position source change
first affects the L1 fingerprint when the ring reaches that position, while
global hash/PRNG controls diverge immediately.

### 4.3 Honest status

| Claim | Status |
|---|---|
| Canonical L1(C) law can live in clean Lua | `GREEN_PARITY` |
| L1 measurements are non-trivial temporal state | `GREEN_LOCAL` for registered controls |
| L1 is a continuing engine across Packet deaths | `DOCUMENT_DECISION`, not museum/runtime proof |
| Production prompt/source adapter is known | `OPEN_LAW` |
| L1 causes better routing or coding | `RED_MISSING` |
| L1 is currently inside Packet body ticks | `RED_MISSING` |

## 5. L2 Audit: TABLE / FORMATION

### 5.1 Museum law and archaeology

The final blueprint selects five glyphs:

```text
CONNECT + DISSOLVE + ENCODE + OBSERVE + CHOOSE
```

Older L2 branches are not mutually identical:

```text
an early "four-node" document used OBSERVE/RUNTIME/CHOOSE/ENCODE
the retrospective later calls a five-item list "four-node"
neural stands modelled local graph activation
the Eva.00 branch discovered hidden CONNECT inside ENCODE
Q# was only a temporary potentiality probe
```

These branches provide evidence about formation. Document 60 is the layer
formula; none of the old stand bodies is automatically canonical.

### 5.2 proc-17 surfaces

| Surface | Current implementation | Status |
|---|---|---|
| Distinguished potential | Versioned units in `runtime/field.lua` | `GREEN_LOCAL` |
| `▽` materialization | One user/carrier unit from birth | `GREEN_LOCAL` as Packet ingress, not L1 physics |
| `☰` recognition | Replaceable bounded `relations.raw` epoch | `GREEN_LOCAL` writer |
| `☷` release | Active relation weakening/dissolution with reason and residue | `GREEN_LOCAL` local API; live producer missing |
| `☴` semantic observation | Substrate response plus upper-eye and field unit | `GREEN_LOCAL` |
| `☴` raw-relation observation | Registry declares relation read; organ does not perform it | `RED_MISSING` |
| `☵` formation | Text/chaos to CALM structure, field units, identity map, explicit loss | `GREEN_LOCAL` current path |
| `☵` relation-guided formation | Does not read `relations.raw` | `RED_MISSING` |
| `☳` collapse | Selection, killed alternatives, activation changes, loss | `GREEN_LOCAL` current CALM path |
| Continuous form emission | No living L1-fed L2 body | `RED_MISSING` |

### 5.3 Honest status

proc-17 does not lack an L2 implementation entirely. It has most required
objects and local rights. It lacks the causal relation lifecycle and live
lower input that make those objects one formation layer.

```text
L2 objects exist
L2 organs exist
L2 vertical physics is not closed
```

## 6. L3 Audit: CRYSTALL / Form Runtime

### 6.1 Museum's final selected law

The latest packet-slop branch says:

```text
RUNTIME = prior/posterior memory
LOGIC   = likelihood/theory function
CYCLE   = repeated update through time
CHOOSE  = posterior selection
ENCODE  = stable form/theory packing
```

It then strengthens the claim:

```text
L3 = self-theorizing Bayesian runtime of forms
```

This was a selected research direction, not a finished canonical runtime.
The retrospective rates L3 at roughly forty percent and preserves substantial
open questions.

### 6.2 proc-17's actual lower body

proc-17 has a different but coherent concrete incarnation:

| Glyph | Current product-specific law | Local evidence |
|---|---|---|
| `☵` | Form semantic response into task-shaped CALM/work units | Encode suites and coding batteries |
| `☳` | Select alternatives and account killed possibility/loss | Choose suites |
| `☶` | Run typed validation spells and stamp current evidence | Logic/spell/foundation suites |
| `☲` | Repeat while body-owned work remains and budget can pay | Cycle/lower-triangle suites |
| `☱` | Reconcile significant runtime frames and expose body progress/economics | Camera/runtime suites |

This is real L3-like machinery. It is not currently Bayesian:

```text
no posterior distribution over forms
no explicit likelihood function
no competing theory heads
no next-form prediction error
```

### 6.3 Decision pressure

The merge must not assume either of these without testing:

```text
assumption A: proc-17 already discovered a non-Bayesian sufficient L3 law
assumption B: the museum's Bayesian/self-theorizing law must be added intact
```

The table decision is narrower:

```text
proc-17 has GREEN_LOCAL L3 operator mechanics.
the generic law binding those mechanics into a runtime of forms remains OPEN.
```

The first vertical L2 form stream will determine whether posterior/theory
machinery is required, useful, redundant, or a museum branch to reject.

## 7. L4 Audit: MANIFEST

packet-slop intentionally left L4 open after rejecting the transformer as the
machine's center.

proc-17 supplies the first concrete L4 body:

```text
reads Packet trace, CALM, validation, cycle, runtime reconciliation, economics
classifies output shape
preserves blocked versus complete outcome
creates manifest record
creates terminal/death/residue
forbids same-life continuation
```

Current status:

| Claim | Status |
|---|---|
| Typed text/code/plan/residue assembly | `GREEN_LOCAL` |
| Rejected validation cannot be laundered as success | `GREEN_LOCAL` |
| Manifestation is terminal and corpse is immutable | `GREEN_LOCAL` |
| Input comes from body trace in current tree tests | `GREEN_LOCAL` |
| L1-originated form survives every seam into L4 | `RED_MISSING` |
| Body writes/tests repository artifact autonomously | `RED_MISSING` product capability |

The absence of hands does not invalidate MANIFEST as L4. It limits the coding
product built around L4.

## 8. Repeated Glyph Matrix

One Lua module name must not erase layer context.

| Glyph | L1 meaning | L2 meaning | L3 meaning | Current proc-17 risk |
|---|---|---|---|---|
| `▽ FLOW` | Movement/emergence inside live medium | Boundary ingress from L1 is implied, not a listed L2 organ | None | Current `organs.flow` is Packet birth, not continuous L1 FLOW |
| `☰ CONNECT` | Continuous positional interaction | Transient relation recognition | None | Raw writer exists; four readers missing |
| `☷ DISSOLVE` | Irreversible local replacement/decay | Release of differentiated relation/form | None | Current organ only consumes active relation |
| `☴ OBSERVE` | None as registry act | Bounded view of L1/relation/form | None in final formula | Current organ equates upper observation with substrate call |
| `☵ ENCODE` | None | Potential/relation into form hypothesis | Pack stable form/theory state | Current implementation mixes formation and CALM production |
| `☳ CHOOSE` | None | Collapse candidate formation paths | Select posterior/active form | Current implementation is CALM-alternative selection |
| `☶ LOGIC` | None | None | Validate/likelihood/theory | Current product validator is concrete but not generic likelihood |
| `☲ CYCLE` | None | None | Repeated form update | Current repeat law is body-progress based |
| `☱ RUNTIME` | None | Boundary view appeared in old branch but not final five-glyph formula | Hold/reconcile living form state | Registry over-declares relation momentum writes |
| `△ MANIFEST` | None | None | Boundary input only | Current terminal implementation supplies L4 v0 |

Layer reuse is recursion only when the active layer and input/output contract
are explicit. Otherwise it is an overloaded function name.

## 9. Body Services Outside The Four Layers

These mechanisms are essential but must not be forced into one layer:

| Service | Role | Current status |
|---|---|---|
| Packet core | One mortal task life, identity, revisions, trace, finality | `GREEN_LOCAL` |
| Full Tree | Legal adjacency and lifecycle direction | `GREEN_LOCAL` |
| Router authority | Derive movement from Packet records | Tree opt-in; promotion incomplete |
| Legacy observer | Historical control/instrumentation | Isolated and ablation-tested |
| Budget/loss | Runtime economics versus identity physics | `GREEN_LOCAL` |
| Camera | Continuous body frames plus explicit runtime reconciliation | `GREEN_LOCAL` |
| Grave/compost | Between-life warning/bequest/statistical memory | Partial readers and lifecycle remain |
| Substrate | Replaceable semantic current | Fake/DeepSeek adapters exist |
| Hands | Capability-bounded external mutation | `RED_MISSING` |
| CLI/TUI | Machine/human session ownership | Design only |

The router revolution and L1 recovery are orthogonal:

```text
router = government
L1     = medium
layers = form lifecycle
```

## 10. Seam Audit

| Seam | Required transformation | Current evidence | Status |
|---|---|---|---|
| `S0 source -> L1` | Production source adapter initializes/perturbs canonical L1 | Museum fixture only | `OPEN_LAW` |
| `S1 L1 -> Packet birth` | Continuing/local L1 yields bounded birth mark or projection | No production integration | `RED_MISSING` |
| `S2 L1 -> L2 TABLE` | Live pre-addressable state becomes bounded distinguished units | Current FLOW reads prompt/carrier directly | `RED_MISSING` |
| `S3 within L2 relations` | Raw relation goes to release, observation, or encode | Raw writer only | `RED_MISSING` causal chain |
| `S4 L2 -> L3` | Form hypothesis becomes living CALM/runtime form | Current ENCODE does this from semantic text, not L1/L2 relation stream | `YELLOW_PARTIAL` |
| `S5 within L3` | Evidence updates form life until completion/death | Product-specific lower triangle works | `GREEN_LOCAL`, generic law open |
| `S6 L3 -> L4` | Ready/blocked form becomes honest output and terminal | Tree manifest tests | `GREEN_LOCAL` |
| `S7 L4 -> next life` | Corpse/carrier creates new Packet with bounded inheritance | Libraries/tests exist; automatic lineage runner absent | `YELLOW_PARTIAL` |

The present machine is strongest at `S5-S6`, exact but isolated at L1, and
weakest at `S1-S4`.

## 11. What Current Green Tests Prove

The cold 2026-07-18 run confirms:

```text
45/45 Lua suites
10/10 tree authority Gate A
7/7 tree instrumentation Gate B
4/4 manifest honesty
8/8 mortality
exact standalone L1 parity
```

They prove:

```text
Packet finality and mortality
local organ contracts under their fixtures
tree/legacy instrumentation isolation
typed route evidence and terminal honesty
standalone L1 law
```

They do not prove:

```text
continuing L1 lifetime
L1 flow marks on live Packets
L1-driven L2 distinction
raw relation consumption by neighboring organs
generic Bayesian/self-theorizing L3
one vertical L1-to-L4 life
```

## 12. Current Contradictions Requiring Explicit Resolution

| ID | Contradiction | Required table owner |
|---|---|---|
| `X1` | L1 freezes with Packet versus L1 outlives Packet | L1 lifetime table |
| `X2` | L1 is seeded per source versus L1 only marks Packet birth | L1 lifetime/source table |
| `X3` | CONNECT is transient versus `relations.active` activation model | L2 relation table |
| `X4` | ENCODE should consume relation motif but current organ ignores raw relations | L2 relation table |
| `X5` | OBSERVE declares relation read but only asks substrate | L2 relation table |
| `X6` | DISSOLVE edge accepts newly detected bad relation but organ requires active relation | L2 relation table |
| `X7` | RUNTIME registry declares active/momentum writes but organ performs neither | L2/L3 ownership crystall later |
| `X8` | proc lower triangle works but museum L3 selects Bayesian theory law | Future L3 experiment after L2 stream |
| `X9` | Full Tree is canonical but default authority remains shadow | Existing promotion corpus, not this merge |

No contradiction is permission to change live code during table stage.

## 13. Assembly Strategy

The correct implementation strategy is not a repository merge.

```text
preserve packet-slop as museum
extract only laws and reproducible oracles
implement every surviving law in proc-17's current Lua ownership boundaries
keep old and new behavior isolated until matched controls pass
promote one seam at a time
```

Order:

```text
1. settle L1 lifetime and birth-mark boundary
2. settle L2 transient relation ownership
3. implement both without route authority
4. grow one deterministic L1 -> L2 formation fixture
5. feed its form into current L3 mechanics
6. observe whether a Bayesian/theory layer is actually required
7. manifest the resulting form through current L4
8. compare against controls with L1/relation use disabled
```

## 14. First Whole-Machine Vertical Slice

The minimum meaningful slice is not a coding benchmark. It is a body-physics
fixture with inspectable intermediate state.

```text
named deterministic L1 source/state
-> one Packet birth carrying a flow mark
-> bounded L1 projection becomes at least two TABLE units
-> CONNECT records one current raw relation
-> one of the three legal CONNECT exits consumes that exact epoch
-> ENCODE produces one retained form with provenance and explicit loss
-> current L3 lower mechanics hold/check/cycle that form
-> MANIFEST emits a typed result and Packet dies
```

Required controls:

```text
same fixture with L1 mark only (no L1-driven semantic claim)
same units with relation use disabled
same form with L3 update disabled
same terminal input with rejected validation
```

Acceptance:

```text
every intermediate writer has a named reader
every transition names exact source event/object versions
no substrate chooses the route
no harness injects the expected intermediate form
truth status never upgrades through structural plumbing
budget/loss/death remain internal
the full trace is canonical and terminal
```

## 15. False-Green Matrix

| False green | Rejecting evidence |
|---|---|
| Same glyph names called one implementation | Layer-specific input/output contracts required |
| L1 parity called body integration | `S1/S2` remain red |
| Current ENCODE output called L2 relation form | Must consume exact raw epoch in matched control |
| Registry declaration called an organ effect | Concrete trace/state mutation required |
| Product lower triangle called Bayesian L3 | Posterior/theory evidence required or museum law explicitly rejected |
| MANIFEST smoke called whole L4 chain | Form must originate from the vertical fixture |
| Old stand copied and tests pass | New implementation must use proc-17 ownership boundaries |
| Router chooses expected path because competitors are excluded | Live competing candidate plus measured witness required |
| All tests green | Seam-specific negative controls still required |
| External auditor agrees | Auditor commentary is not runtime evidence |

## 16. Table Decision

The audit authorizes the next two table artifacts:

```text
Table A: l1_continuing_flow_birth_mark_yellowprint.v0.md
         continuing L1 lifetime and Packet birth-mark boundary
Table B: l2_transient_relation_lifecycle_yellowprint.v0.md
         L2 transient relation lifecycle and four CONNECT roads
```

Both table artifacts were assembled on 2026-07-18. Their existence closes the
audit-writing step; it does not change any runtime status in Sections 3 or 10.

It does not authorize:

```text
L1 body integration
relation ownership rewrite
RUNTIME activation removal
router promotion
Bayesian L3 implementation
repository hands
```

External review may challenge these tables, but the project no longer depends
on one specific reviewer or model session. The sources, falsifiers, and cold
commands needed to reconstruct the audit are contained in the repositories.
