# DISSOLVE Pressure-Relief Pause And Resume Contract

```text
layer: CHAOS
date: 2026-08-14
purpose: exact pause marker before a new brainstorming branch
published baseline: de33611c0037
branch: main
published baseline status: pushed to origin/main
runtime implementation after baseline: R8.1-R8.2 schema and selected-obligation resolver
working tree: intentionally dirty with pressure-relief test and documents
router/default authority change: none
CRYSTALL: R7 completed 2026-08-20
CRYSTALL result:
  docs/02_crystall/blueprints/dissolve_pressure_relief.v0.md
next exact gate: R8.3 effect, final-state and old-readiness join
active execution plan:
  docs/00_chaos/dissolve_pressure_relief_execution_and_codex_budget_plan_2026-08-14.md
```

## 0. Resume In One Screen

The first direct DISSOLVE treatment is complete and published:

```text
real QA rejection
-> terminal corpse
-> lineage recovery
-> bounded carrier
-> NETWORK projection
-> FLOW materializes current work + inherited rejected form
-> qualified pressure selects ▽ -> ☷
-> atomic release preserves residue with zero irreversible identity loss
-> actual successor route ☷ -> ☴
-> exact P10 reader joins route, effect, field and no-rigidity control
```

After publishing that treatment, the immediate post-release pressure was
measured. The first interpretation contained a topology false green. The
experiment was corrected, and a TABLE contract now describes the future pure
reader.

Current stopping point:

```text
CHAOS measurement: complete
topology false-green diagnosis: complete
corrected executable experiment: green
TABLE reader contract: crystallized
cross-table audit R1-R5: complete 2026-08-20
TABLE amendment R6: complete
CRYSTALL R7: complete 2026-08-20
runtime reader module: selected obligation resolves; effect join pending
general semantic GC: not implemented
```

Audit and amended contract:

```text
docs/00_chaos/dissolve_pressure_relief_cross_table_audit_r1_r5_notes_2026-08-20.md
docs/01_table/yellowprints/dissolve_pressure_relief_reader_yellowprint.v0.md
```

## 1. Published Runtime Baseline

Commit `de33611c0037` contains the completed direct inherited-form campaign:

```text
qualified inherited-form release witness/action
atomic unit_dissolution + residue transaction
exact ▽ -> ☷ -> ☴ runner life
consumer ablation
observer masslessness
hostile rollback/finality controls
P10 v1 evidence reader and no-rigidity control
```

Permanent published evidence includes:

```text
tests/test_inherited_form_dissolve.lua
tests/test_inherited_form_dissolve_hostile.lua
tests/test_inherited_form_dissolve_life.lua
tests/test_dissolve_p10_evidence.lua
docs/03_manifest/qualified_dissolve_inherited_form.v0.md
docs/03_manifest/dissolve_p10_evidence.v0.md
```

No runtime module has changed after this commit.

## 2. The Measured Aftermath

The bounded treatment exposes one selected pre-effect obligation:

```text
kind: inherited_rejected_form_release_need
source coordinate: ▽
target: ☷
causal class: blocking_demand
selected witness count: 1
```

The release effect changes the exact inherited form:

```text
activation: live -> dissolved
version: N -> N+1
released_mass: {forms=1, relations=0}
irreversible_identity_loss: 0
bounded rejected-form residue: present
same release readiness after effect: already_released
```

The actual post-effect body then exposes a different obligation:

```text
kind: upper_observation_need
actual coordinate: ☷
target: ☴
source domain: upper_observation:material+semantic
scope: current work + dissolved form + bounded residue
```

In the controlled sample:

```text
selected release obligation: 1 -> 0
successor OBSERVE obligation: 0 -> 1
aggregate qualified witness count: 1 -> 1
```

This falsifies aggregate witness-count delta as a general definition of
`pressure_relief`. DISSOLVE can discharge one obligation while exposing a new
and legitimate obligation. It does not promise global calm.

## 3. The Topology False Green

The initial comparison used:

```text
before: actual coordinate ▽
after:  actual coordinate ☷
```

That comparison was insufficient. A witness targeting `☷` disappears as soon
as the Packet arrives at `☷`, even before the release executes, because
`☷ -> ☷` is not a valid transition.

The corrected experiment proves both facts separately:

```text
arrival at ☷ before effect:
  actual-coordinate release witness = absent
  same-coordinate ▽ control witness = still present with the same witness_id

after exact release:
  same-coordinate ▽ control witness = absent
  same-obligation witness = absent
  old action = stale
  readiness = already_released
  real-coordinate ☷ successor = upper_observation_need -> ☴
```

Therefore route movement alone receives no relief credit. The causal
comparison must hold the source topology coordinate fixed while reading the
real post-effect body state.

## 4. Current Uncommitted Slice

The working tree intentionally contains:

```text
new:
  docs/00_chaos/dissolve_pressure_relief_first_measurement_notes_2026-08-13.md
  docs/01_table/yellowprints/dissolve_pressure_relief_reader_yellowprint.v0.md
  tests/test_dissolve_pressure_relief.lua

amended:
  docs/00_chaos/dissolve_campaign_conservation_and_resume_notes_2026-08-12.md
  docs/03_manifest/current_state.md
  tests/run.lua
```

This slice has not been committed or pushed. Do not mistake the TABLE contract
for implemented runtime authority.

## 5. Verification State

Observed on this working line:

```text
full suite after initial measurement test: 137/137 passed
mortality battery: 8/8 passed
corrected same-coordinate focused test: passed
luac -p corrected test: passed
git diff --check: passed
```

The corrected test added only pure derivations and assertions after the full
run; production code did not change. A later commit gate may repeat the full
suite if desired.

## 6. TABLE Decision Now On The Desk

The drafted yellowprint is:

```text
docs/01_table/yellowprints/dissolve_pressure_relief_reader_yellowprint.v0.md
```

Its central decisions are:

```text
reader runs after the effect
reader is pure and detached
reader writes no Packet trace, cache, watermark or second ledger
caller supplies source refs, not witness payloads or counts
selected route/action/effect/final-state identities must join exactly
same-coordinate post derivation isolates body change from route movement
actual-coordinate post snapshot reports successor obligations separately
missing evidence means not_measurable, never zero
trusted contradiction stays a loud world/harness failure
v0 supports exactly one selected inherited-form release obligation
```

The proposed output is a structured causal discharge view:

```text
selected obligation identity
exact effect identity
controlled post state
real successor state
discharged obligation count
unresolved selected obligation count
aggregate counts marked diagnostic_only
```

It is deliberately not a universal scalar `Z` formula.

## 7. Exact Resume Order

The sequence remains:

```text
R1 cross-audit TABLE against qualified pressure/action composition   complete
R2 cross-audit route/arrival and trace ordering                      complete
R3 cross-audit atomic DISSOLVE effect/final-state joins              complete
R4 cross-audit runtime camera and post-effect snapshot ordering      complete
R5 cross-audit upper OBSERVE successor classification                complete
R6 amend TABLE for every real finding                                complete
R7 authorize and write CRYSTALL                                      complete
R8.1 strict request/error/view schemas                               complete
R8.2 body-trace resolver and selected-obligation join                complete
R8.3 effect, final-state and old-readiness join                       next
R8.4-R8.6 complete pure reader and runner hook                       pending
R9 add hostile topology/effect/identity/masslessness controls
R10 run focused, full, mortality and static gates
```

The audit must explicitly test the false-green control:

```text
route arrives at ☷
effect does not execute
actual-coordinate old witness is absent
same-coordinate old witness remains present
result must not be called relief
```

## 8. Forbidden Shortcuts

```text
do not write pressure_relief into unit_dissolution
do not compare snapshots at different coordinates without a control
do not use route arrival as proof of effect
do not use aggregate witness-count delta as causal discharge
do not subtract successor OBSERVE pressure from completed DISSOLVE relief
do not accept caller-built witness/count payloads
do not create a mutable aftermath ledger
do not let a counterfactual control snapshot route the living Packet
do not turn missing evidence into zero relief
do not convert Lua/invariant failures into Packet mortality
do not generalize this direct-unit treatment to semantic GC without TABLE work
```

## 9. Deferred Larger DISSOLVE Work

Still outside the current reader slice:

```text
semantic support roots
support opportunities and derived age
mark/sweep eligibility
raw-relation and formed-relation collection
multiple simultaneous release obligations
non-zero irreversible DISSOLVE loss
general calibration of Z
cross-generation pressure-relief aggregation
compost reader for old release history
production L1 distinction projection feeding CONNECT
claim that release causes later semantic insight
default router/full-tree promotion
```

The old raw-stale route remains red because the ordinary body still lacks the
required writer. The direct inherited-form treatment does not silently solve
that larger problem.

## 10. Branching Rule For The Next Discussion

The discussion beginning after this checkpoint may produce a new hypothesis,
experiment or architectural branch. It does not supersede this DISSOLVE line
unless a later document says so explicitly.

To return here, the minimal instruction is:

```text
read dissolve_pressure_relief_pause_and_resume_notes_2026-08-14.md
resume at R8.3 effect, final-state and old-readiness join
```
