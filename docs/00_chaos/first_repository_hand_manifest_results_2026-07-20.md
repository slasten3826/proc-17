# First Repository Hand Manifest Results

Date: 2026-07-20

Status:

```text
roadmap 7.10 complete
repository-hand chapter 7.1-7.10 complete
first full production-provider Packet life runtime-confirmed
default router authority unchanged (shadow)
```

## Result

The terminal reader gap is closed. One opt-in Tree Packet can now move from a
strict repository work proposal to a real bounded filesystem effect and then
deliver a deterministic body-owned result through △.

Observed singular life:

```text
route:  ▽ -> ☴ -> ☵ -> ☱ -> ☶ -> ☱ -> △
ticks:       ☴    ☵    ☱    ☶    ☱    △
status: dead
death:  complete
manifest mode: repository_delivery
output protocol: repository.result.v0
```

Observed alternative life:

```text
▽ -> ☴ -> ☵ -> ☴ -> ☳ -> ☶ -> ☱ -> △
```

It performs one real choice and manifests exactly one selected artifact.

## What △ Reads

`runtime/repository_result.lua` receives no host service. At readiness, run and
qualified-effect verification it reconstructs the same result from:

```text
current field unit/version and exact public action
☶ attempt, receipt, independent verification and accepted validation
☱ work_completion event
current repository progress with remaining=0
```

The output contains one relative path, operation, observed byte length,
observed SHA-256 and exact provenance refs. It contains no raw content,
absolute path, provider state, handle, registry, lease, command or shell.

The result id is a deterministic digest. Runner tick text changes do not change
the projection. Mutating the returned tick payload does not mutate the sealed
Packet manifest.

## Physics And Economics

MANIFEST does not repeat the filesystem read. The repository effect remains one
centrally charged event:

```text
tool_calls = 2
file_writes = 1
repository_effect budget events = 1
△ adds one ordinary step
△ identity loss = 0
```

After manifestation, `packet.manifest_packet` records the manifest, begins the
terminal boundary and freezes one `dead/complete` corpse.

## Negative Evidence

The matched controls prove:

```text
before work_completion       no repository delivery witness
delivery reader ablated      completion exists, repository manifest absent
rejected read-back           no completion and no complete repository result
stale field version          committed delivery becomes not-ready
changed runner text          output remains byte-identical canonical JSON
default shadow authority     no repository delivery and no Tree commitment
```

The M0-M10 suite was first recorded at `0/11` because the reader module was
absent. It is now `11/11` green.

## Production Boundary

The fake provider is not the only end-to-end witness. `REAL1` grows a Packet
against the production C provider inside an identity-owned temporary repository:

```text
atomic create-no-replace
independent bounded read-back
exact LOGIC acceptance
exact ☱ completion
repository result projection
△ manifestation
dead/complete
explicit grant revoke before fixture cleanup
```

No caller spell, caller path execution or harness-owned completion is used.

## Verification

```text
registered Lua corpus                     91 suites, all green
repository hand battery                   14/14 green
repository manifest M0-M10                11/11 green
repository route R0-R10                   11/11 green
repository hostile audit                  16/16 green
repository effect                         14/14 green
repository progress                        9/9 green
real Linux effect/route lives               3/3 green
mortality                                  8/8 green
native full fault test                       green
GCC -fanalyzer production + test             green
ASan + UBSan                                 green (LeakSan not claimed)
all Lua syntax                               green
git diff --check                             green
```

One environmental P15 cross-device bind-mount case remains an explicit skip
when `PROC17_TEST_BIND_MOUNT` is not configured. It is not counted as a pass.

## Honest Boundary

This is a complete life for one narrow operation, not a complete coding agent.
V0 can create one absent UTF-8 text file. It cannot patch, overwrite, delete,
rename, create directories, search/read arbitrary repository files, run tests,
invoke git or schedule a multi-file transaction. Tree authority remains opt-in.

The victory is exact: proc-17 has crossed the real-world boundary and returned
through △ without moving authority into the model or lying about evidence.
