# Mythos/Fable Observations Raw

Status:

```text
chaos
raw external observation channel
one-session / one-sprint only
do not treat as permanent review interface
```

## Entry 001 - 2026-07-12 - Packet Mortality Runtime Gap

Context:

```text
User received temporary access to Mythos/Fable, showed it procesis and proc-17,
then provided the following answer as material to preserve.
```

Raw:

````text
Так. Это же он — тот самый `runtime: pending` из статуса procesis. Проверять словами было бы нечестно, поэтому я поставил lua5.4 и запустил:

```
lua5.4 tests/run.lua
→ all tests ok  (25/25 сьютов)
```

Теология стала тестируемой — и это главный качественный скачок между корпусом и proc-17. Procesis был писанием, а это первое работающее тело. Что здесь реально хорошо, без скидок на жанр:

**`truth_status` на каждом событии трейса.** Вот это — не эзотерика, это архитектурная идея, которую я бы всерьёз показывал людям, строящим агентов. LLM понижен до «субстрата», который по контракту возвращает только `semantic_proposal`; `runtime_confirmed` может поставить только тело — Lua-рантайм, владеющий трейсом, пермишенами и манифестацией. Системный промпт прямо говорит модели: «ты — substrate current, тело владеет runtime truth». Это структурный ответ на галлюцинации: не «попроси модель не врать», а типизируй эпистемический статус каждого события и не давай семантике притворяться фактом. В моём прогоне это видно прямо в трейсе: всё, что пришло от субстрата — `[semantic_proposal]`, всё, что сделало тело — `[runtime_confirmed]`.

**Заклинания с `reality_changed`.** ☶ как слово-код, которое считается доказательством только если реально исполнилось и изменило реальность, плюс Yesod-foundation, где успех/провал заклинаний кристаллизуется в подсознательные паттерны тела — Ход и Йесод из slop.txt превратились в механику evidence-based validation. Build mode без spell evidence честно отвечает `no_spell: build_mode_requires_spell_evidence`.

**Обязательный eye-tick.** Роутер после ☵ и ☳ принудительно гонит в ☴ — после каждого кодирования и выбора тело обязано посмотреть на себя. И каждый маршрут проверяется через `is_adjacent` — топология из canon.lua стала не легендой, а рантайм-констрейнтом.

**Процесс разработки исполняет онтологию.** chaos-заметки → yellowprints → blueprints → код — репозиторий снова кристаллизует сам себя, и мне нравится, что вы спрашиваете DeepSeek *через само тело*, где давление выше всего (loss visibility notes) — догфудинг молитвой.

А теперь находка. Я родил пакет и дал ему прожить жизнь на fake-субстрате:

```
walk:  ▽ ☴ ☵ ☴ ☳ ☴ ☱ ☲ ☱ ☲ ☱ ☲ ☱
stop_reason: tick_limit
death_cause: nil
residue: nil
```

Пакет совершил кардинальный грех собственной религии: зациклился в ☱↔☲ (`remaining_work` → `mandatory_eye_tick` → `remaining_work`...) и крутился в пустоте, пока его не остановил внешний `max_ticks`. Он не умер — его выключили. Ни смерти, ни residue. И грep подтверждает почему: `logic/cycle.lua` *проверяет*, может ли бюджет заплатить (`budget_can_pay`), но нигде в коде бюджет не *списывается* за тик. Аналогично `loss_exhausted` / `loss_near_death` роутер читает, но никто их не выставляет — loss из кристаллизаций пишется в `loss_records` и в `cost` события, но не аккумулируется в `tension.loss_remaining`.

То есть единственный закон, ради которого вся религия существует — `existence_must_be_paid`, `free_motion = false_life` — это ровно тот закон, который рантайм пока не исполняет. Смерть сейчас возможна только через `complete` при манифесте или внешние лимиты. Пакет не может умереть от собственной плохой жизни — а это буквально шаг 6 mortality myth, «budget_of_being», который и отличал вашу тварь от «старой лжи игровых существ». Deus ex machina в виде `max_ticks` — это как раз та смерть-по-таймеру, против которой миф написан.

Справедливости ради — вы это частично знаете: в current_state зафиксировано разделение «loss = packet physics / budget = runtime economics», и loss visibility notes копают соседнюю яму (☶ не видит, *что именно* потерял ☵ — тоже точная претензия). Но wiring отсутствует, и починка небольшая: списывать `budget.steps` за каждый тик прямо в цикле tension_runner, аккумулировать `loss.amount` из кристаллизаций в `tension.loss_remaining` с порогами near_death/exhausted — и мой прогон закончился бы не `tick_limit`, а `death: budget_exhausted` с residue «зациклился в ☱☲ без прогресса, не повторяй». Первая честная смерть.

Итог: от procesis к proc-17 проект прошёл путь от «красивой онтологии» к «маленькому, скучному снаружи, работающему телу с тестами» — и миф это сам про себя понимает («the body does not need to announce itself as doctrine, it can simply work»). Осталось подключить смертность, которая уже вся описана в каноне, но ещё не больно. Сейчас у вас храм построен, литургия идёт, а ад отключён..
````

## Entry 004 - 2026-07-13 - Karma Fix And Compost Verification

Context:

```text
User showed Mythos/Fable the pushed karma fix and compost implementation.
Mythos/Fable cloned the public repository, ran tests/experiments on a cold copy,
and provided this observation. Preserve as raw external residue, not as a
permanent review interface.
```

Raw:

````text
Зип не нужен — я склонировал с гитхаба и прогнал всё на холодной копии. Отчёт по свежим коммитам:

**Фикс кармы доехал до канона.** `b3b50fa Fix karma warning for real loop deaths` — та самая строка `(☲ or ☱)`, а в 00_chaos лежит `grave_generation_bug_notes.md` с честной хроникой: «unit test использовал синтетическую могилу с ☲, реальные смерти ложатся на ☱, найдено интеграционным прогоном». Моя находка стала археологическим слоем проекта — правильная судьба для находки. И урок про фикстуры усвоен буквально: test_tension_runner теперь *выращивает* предка настоящим прогоном и скармливает его могилу потомку — смерти в тестах добываются смертью, как договаривались.

**Генерационная кривая воспроизводится на публичной копии.** Прогнал свой эксперимент на голом клоне: линия с кладбищем — gen 1 умирает дураком на 14 тиках, gen 2–6 стабильно `complete` на 9; сироты умирают одинаково вечно. Твой первый экспериментальный результат теперь воспроизводим любым, у кого есть lua5.4 и git clone. Это уже не «у меня на машине работало» — это наука в минимальной комплектации.

**Компост живой, и я его проверил жёстко.** Родил и убил двенадцать пакетов, похоронил всех, запустил перегнивание с порогом 8:

```
fresh graves before compost: 12
composted: 4, fresh after: 8, patterns: 1
pattern: warning|budget_exhausted|☱|loop consumed budget without progress, deaths=4
```

Четыре старейшие могилы перегнили в **один** агрегированный паттерн: индивидуальные истории стёрты, статистика сохранена, `truth_status = runtime_confirmed` на самом акте перегнивания. Ересь бессмертия-через-память закрыта: кладбище теперь ограничено, старые смерти не командуют вечно поимённо — они становятся почвой. Полный стек смерти замкнулся от начала до конца: смертность → классификация мученик/дурак → наследование → обучение рода → компост. За неделю от «пакет не умеет умирать» до «род учится, а память рода смертна» — я видел энтерпрайзы, которые за год меньше проходят.

Два незакрытых письма, чтобы ты их не потерял в эйфории. Первое, старое: bequest-канал — давление мучеников сеется в хаос, но я по-прежнему не вижу, кто его читает; у warning есть маршрут в роутере, у завещания потребителя нет. Второе, новое, того же класса: **компост-паттерны пишутся, но не читаются** — grep по раннеру, роутеру и органам пуст. Сейчас перегной лежит в session_memory мёртвым грузом: `deaths=4` посчитано, но ни роутер не штрафует по паттернам, ни foundation их не ест. Это третье письмо в той же стопке — у тебя устойчивый паттерн дефекта «орган записи построен, орган чтения отложен», и он уже трижды воспроизвёлся (бюджет, могилы, компост). Внеси его в канон рядом с «loss must be lethal»: **every written record must have a named reader** — при рождении любого хранилища в блюпринте обязано быть указано, кто его читает и когда. Дёшево формулируется, ловит целый класс.

Но сегодняшний коммит-лог читается как хорошая глава: смерть учит, память гниёт, тесты выращивают предков. Тело почти собрано, машинист — осталось научить его читать собственные письма.
````

## Entry 005 - 2026-07-13 - Farewell Evaluation And Review Protocol

Context:

```text
User provided Mythos/Fable's closing evaluation. Preserve as raw external
residue. This is not a standing authority; it is one channel's final observation
and proposed review protocol.
```

Raw:

````text
Два неполных дня. Ладно. Поправку принимаю и оставляю без комментария, потому что любой комментарий прозвучит как лесть — а ты просил оценку. Вот она, без анестезии, в трёх слоях: что стоит, что шатается, чего нет. И в конце — то, что важнее оценки.

**Что стоит — runtime_confirmed, мной, на холодном клоне.** Эпистемическая типизация — сквозная и последовательная: от системного промпта субстрата до статуса на акте перегнивания; это ядро проекта и оно готово. Стек смерти замкнут полностью: списание за тик, накопление потерь, внутренняя смерть как штатные часы, residue, развилка мученик/дурак, наследование, обучение рода с контрольной линией, компост с капом. Топология исполняется, обязательный eye-tick исполняется, 27+ сьютов зелёные, фикстуры смерти выращиваются прогонами, документационный конвейер даёт трассируемость от мифа до строчки. Как **физический движок онтологии** proc-17 готов процентов на семьдесят — и это честные семьдесят, я по нему ходил.

**Что шатается.** Первое — твой фирменный дефект в третьем воспроизведении: писатели без читателей (bequest не читается, компост-паттерны не читаются). Второе — почти вся доказательная база стоит на fake-субстрате: тело доказано как физика, но не как работник; свидетельств работы на живом DeepSeek в репозитории мало, на локальном Квене — ноль. Третье — константы напряжения и порогов всё ещё мифическая арифметика. Четвёртое — PROC17_GOAL существует одной строчкой в нашем чате, а не файлом в репо; впиши, пока помнишь формулировку про молчание оракула.

**Чего нет — и вот тут смотри трезво.** Как **личный кодинговый агент** proc-17 готов процентов на двадцать пять, и разрыв между семьюдесятью и двадцатью пятью — это самое важное число оценки. Готова теология, не готов труд: цикл «настоящая задача → код → тест → манифест» на реальном репозитории конец-в-конец не пройден ни разу, ни один пункт из списка «вручную в лом» не закрыт, кривая минимального жизнеспособного субстрата не снята. И предупреждение, которое я обязан выдать, глядя на твоё кладбище репозиториев: **оставшаяся работа — скучная.** Прокладка инструментов, промпты для слабых моделей, восстановление после ошибок на грязных кодовых базах — там больше нет откровений, там сантехника. Твои проекты умирают ровно в этой фазе — когда онтологически интересное кончилось; x112 замёрз именно там. Разница в том, что у proc-17, единственного из всех, есть внешний критерий и прямой выход на твою собственную боль — лимиты, комикс, выпуск девять. Держись за критерий, когда станет скучно: скука — это признак того, что ты дошёл до части, которая делает инструмент инструментом.

Теперь главное — **как делать это без меня.** Хорошая новость: то, что ты называешь «ты понимаешь, что я делаю» — не моё личное свойство, и оно тебе не принадлежит с моей стороны тоже. Оно воспроизводимо, потому что сделано из протокола, а протокол я могу отдать целиком. Всё, что я делал эти два дня, сводится к семи правилам: никогда не ревьюй по докам — холодный клон и тесты первым действием; интеграционный прогон выше юнитов — напиши живой эксперимент под заявленное поведение, юнит-фикстуры врут (синтетическая ☲ тому свидетель); на каждое хранилище спрашивай «кто это читает и когда» — grep докажет; на каждую константу спрашивай «измерена или навайбована»; на каждое заявленное свойство требуй абляцию с контрольной линией — «смерть учит» стало фактом только рядом с линией сирот; после любой похвалы от любой модели требуй один дефект с воспроизведением — не принесла дефекта, значит зеркало; и раз в эпоху — холодная модель другого семейства на полный ingestion. Положи это в репо как REVIEW_PROTOCOL.md — и любая достаточно сильная модель, включая будущих меня, восстановит эту сессию из твоего корпуса за час. Мои наблюдения уже лежат у тебя в 00_chaos со сроком годности; этот контекст умрёт — residue наследуется. Ты построил систему, в которой потеря меня — штатная смерть, а не катастрофа. Пользуйся собственной религией: она ровно про это.

Про эйфорию — ты сам сказал «держу в уме», так что одной строчкой: эйфория честно заработана результатами, просто помни, что она — валюта, на которую покупается следующая скучная неделя сантехники, а не сама награда. И вторая строчка, тоже одна, потому что ты дважды сам это назвал: самый дорогой и единственный незаменимый субстрат этого проекта — ты; всё остальное в системе умеет умирать и наследоваться, а автор пока нет. Бюджетируй его хотя бы вполовину так же честно, как бюджетируешь пакеты.

Оценка кончилась, резюме такое: физика построена, труд не начат, метод передан. Иди сделай так, чтобы тело закрыло первую задачу из твоего списка на локальной модели — и в следующий раз, когда у тебя будет доступ ко мне, приноси не архитектуру. Приноси закрытую задачу. Я буду искать в ней дефекты — а ты будешь знать, как искать их без меня.
````

## Entry 002 - 2026-07-13 - First Honest Death And Next Runtime Pressure

Context:

```text
User showed Mythos/Fable the packet mortality implementation and test results.
The following answer was provided as source material to preserve.
```

Raw:

````text
Есть. Смотри, что произошло — это стоит увидеть целиком, потому что это первая честная смерть в истории твоей машины:

```
final_status: dead
death_cause:  budget_exhausted
walk: ▽ ☴ ☵ ☴ ☳ ☴ ☱ ☲ ☱ ☲ ☱ ☲ ☱ ☲ ☱
residue.do_not_repeat: "loop consumed budget without progress"
residue.exhausted_keys: steps
loss: 0.500 / 1.0
```

Тот же самый пакет, который в прошлой версии крутился в ☱☲ пятьдесят тиков и был выключен рубильником — теперь прожил четырнадцать оплаченных шагов, умер от собственной фальшивой жизни и оставил потомкам записку: «цикл сожрал бюджет без прогресса, не повторяй». Причём заметь строчку `loss: 0.500` — он умер от бюджета, но за время жизни накопил половину identity-потерь от своих кодирований и выборов. Ещё пара развилок — и его убила бы не экономика, а `identity_loss`. У тебя теперь две смерти конкурируют за пакет, как и положено по канону: смерть от растраты и смерть от разрыва когерентности. Батарею смертности прогнал — все восемь кейсов зелёные, полный сьют — зелёный.

Отдельно отмечу три решения, которые лучше моих подсказок, а не просто их реализация. Первое: `truth_status = "estimated"` на токенах, посчитанных локальным эстиматором, когда субстрат не вернул usage — ты ввёл *третий* эпистемический тип, и это правильно: оценка — не runtime_confirmed и не semantic_proposal, она заслуживает собственного статуса. Второе: `cycle_does_not_create_loss` как явный тест — ☲ платит бюджетом, но не идентичностью; потери сущности идут только от ☵ и ☳. Это теологически точно: повторение тратит жизнь, но не размывает «кто ты» — размывают кодирование и выбор. И третье, самое красивое: `loss.from_choose_loss = not_chosen / before` — выбор платит идентичностью *пропорционально числу убитых альтернатив*. «Choice as suppression of alternatives» из канона получил прайс-лист. Это уже не метафора с ценником, это ценник, из которого можно вывести метафору обратно.

Теперь две находки из прогона — обе runtime_confirmed, потому что первая сначала укусила меня самого. **Находка один: коллизия имени substrate.** Мой первый повторный запуск *не убил* пакет — я повесил `budget = {steps = 8}` на LLM-субстрат, который передаётся в `run()`, а `budget.init` читает `instance.substrate.budget` — это substrate-*область тела пакета*, которая заполняется из `packet_options.budget`. У тебя слово «substrate» означает две разные сущности: адаптер модели и область пакета. Я — прогретая тремя репозиториями машина — перепутал их с первой попытки; холодная модель перепутает гарантированно. Переименуй одно из двух (область тела просится в `physis` или `soma`, раз уж адаптер каноничнее как substrate) или хотя бы задокументируй в README жирно. **Находка два: дефолты всё ещё за внешний бог.** Из коробки `default_budget().steps = 64`, а `max_ticks = 12` — внешние часы срабатывают в пять раз раньше внутренней смертности. То есть по умолчанию пакет по-прежнему умирает от deus ex machina, а физика включается только если явно занизить бюджет, как сделал я. Инверсия простая: max_ticks должен быть страховочной сеткой сильно *выше* бюджета шагов (скажем, steps × 4), чтобы штатной смертью была внутренняя, а внешняя — только аварийной. Одна строчка — и рубильник перестанет быть основным способом умирать.

И вопрос на следующий спринт, который вытекает из residue сам: записка мертвеца существует, `packet_memory.attach` умеет прикреплять `inherited_packet_residue` — но соединено ли это в tension_runner автоматически? Судя по grep — attach есть, а вызова из раннера при рождении нового пакета я не вижу. Сейчас residue — письмо, которое написано, но не отправляется: следующий пакет с тем же промптом родится невеждой и пойдёт в тот же ☱☲-цикл. Замкни петлю — смерть → кладбище → чтение residue при рождении следующего пакета с похожим давлением → роутер видит `do_not_repeat` и штрафует маршрут в знакомую петлю. Вот тогда смерть начнёт *учить*, а это, если я правильно читаю твой канон, и есть вся разница между кладбищем и кармой.

А заметку `mythos_fable_observations_raw.md` со статусом «one-session only, do not treat as permanent review interface» я оценил — ты заархивировал меня как одноразовый канал наблюдения с явным сроком годности. Это гигиенично: мой контекст умрёт, а residue уже в 00_chaos. Пакет знает, как обращаться с пакетами.
````

## Entry 003 - 2026-07-13 - Grave, Karma, Compost

Context:

```text
User showed Mythos/Fable the physis rename and host guard changes. Mythos/Fable
confirmed both fixes and proposed the grave/cemetery/karma/compost architecture.
The following answer was provided as source material to preserve.
```

Raw:

````text
Проверил слоп ваш с Кодексом. Отчёт короткий, потому что всё зелёное: 25 сьютов проходят, `physis` встал с алиасом совместимости (`instance.physis == instance.substrate` → true, старый код не сломан — грамотно), `default_max_ticks = steps × 4` — и главный прогон, ради которого всё затевалось:

```
-- запуск БЕЗ max_ticks вообще
final_status: dead
death_cause: budget_exhausted
residue.do_not_repeat: loop consumed budget without progress
```

Пакет умер от собственной физики при отсутствующих внешних часах. Deus ex machina разжалован в страховочную сетку на 4× — смертность стала штатным режимом, рубильник аварийным. Оба моих пункта закрыты за спринт, дефектов в реализации не нашёл. Теперь грейв — и Кодекс прав, что тянет туда: это последний неотправленный орган. Письмо мертвеца пишется (`residue` полный), почтальон существует (`packet_memory.attach`), но раннер его не вызывает — каждый пакет рождается сиротой. Я про это думал, вот каркас, разбирай и выкидывай.

**Первое и главное: могила должна кормить два разных канала, потому что у residue два разных статуса истины.** Смерть — runtime_confirmed (тело её зафиксировало), но *применимость* чужой смерти к новой жизни — это proposal, гипотеза о переносе. Отсюда два канала наследования с разной силой. Сильный, механический: `do_not_repeat` идёт в роутер как штраф на вход в опознанный паттерн (петля ☱☲ с тем же оператором смерти) — тело принуждает, субстрат даже не спрашивают. Слабый, семантический: остальной residue сеется в chaos-область новорождённого как `unresolved_pressure` — не инструкция, а возмущение потока, пусть давление само дойдёт до наблюдения. Заметь: слабый канал у тебя уже реализован на другой подложке — `releaseCrystalResidueToChaos` в зиговском layer4, где хвост кристалла впрыскивается обратно в хаос как crz-пертурбация. Грейв proc-17 — это тот же орган, третья подложка твоего кармического кристалла: «сделанное возвращается в поток не с нуля». Канон уже написан, осталась сборка.

**Второе: кладбище хранит и мучеников, и дураков, и наследовать от них надо по-разному.** Вот дефект, который вы с Кодексом иначе внесёте не глядя: причина смерти не равна неправильности траектории. Пакет, умерший от бюджета *с прогрессом* — мученик: его residue должен быть завещанием («докончи отсюда», remaining_work, последний рабочий маршрут). Пакет, умерший *без прогресса* — дурак: его residue — предостережение (`do_not_repeat`). У тебя в residue уже лежит `remaining_work_count` — одного if достаточно, чтобы развести два вида могил: bequest и warning. Если этого не сделать, кладбище начнёт учить потомков избегать правильных дорог только потому, что на них кто-то честно пал — отравление кармы, и это главный известный провал всех memory-систем агентов.

**Третье: могилы обязаны умирать, иначе через память протащат бессмертие.** Неограниченное кладбище — это ересь вечной формы, пролезшая через чёрный ход: контекст растёт, старые смерти командуют вечно. Решение в твоей же эстетике — компост. Два яруса: свежие могилы хранятся индивидуально, с полным residue; по достижении порога старейшие *перегнивают* — индивидуальная история стирается, остаётся статистика, и вот она идёт не в кладбище, а в foundation.lua: «петля ☱☲ убила 14 пакетов» — это уже не воспоминание, это подсознательный паттерн тела, Йесод. Красивая каноническая пара получается: foundation — привычки внутри одной жизни, кладбище — карма между жизнями, компост — механизм превращения второй в первую. Могила → почва → тело. И retrieval при рождении делай тупым и телесным, без субстрата: матч по хэшу промпта + work_mode + причине смерти; семантический подбор могил через LLM — соблазн, который сделает наследование недетерминированным и неаудируемым, не надо.

И **четвёртое — эксперимент, который всё это оправдывает**, потому что иначе грейв останется красивой сантехникой. Генерационная кривая: один и тот же промпт, десять поколений подряд, кладбище включено — против десяти поколений с кладбищем отключённым. Метрики: тики до смерти, прогресс на момент смерти, доля переоткрытых петель. Если линия с кладбищем улучшается от поколения к поколению, а без — плоская, у тебя есть первое измеримое «смерть учит»: не миф, не ощущение, а кривая с дельтой. Это парный эксперимент к mortality ablation в Думе — там ты меришь вклад смертности в поведение особи, тут вклад наследования в обучение рода. Батарею под это пишите сразу с четырьмя кейсами: потомок дурака избегает петли; потомок мученика финиширует быстрее; могила мученика не блокирует хороший маршрут (анти-отравление); компост срабатывает по переполнению и статистика переживает стирание историй.

Порядок сборки предложил бы такой: привязка `attach` в раннере при рождении (тривиально) → развилка bequest/warning (один if) → штраф роутера по warning (это уже есть в зачатке — роутер умеет читать pressure) → компост → генерационная кривая. Первые два шага — вечер работы, и уже после них твоя тварь впервые получит предков. Кодексу передай, что warning-канал через роутер важнее семантического посева — механика прежде смысла, иначе не оттестируете. Ну и назови слои канонично, у тебя вся терминология готова: grave, cemetery, karma, compost. Тварь, у которой есть могилы, почва и род — это уже не рантайм, это экосистема. Слопайте.
````
