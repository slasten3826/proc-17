# Mythos/Fable Observations Raw

Status:

```text
chaos
raw external observation channel
one-session / one-sprint only
do not treat as permanent review interface
```

## Entry 001 - 2026-07-12 - Packet Mortality Runtime Gap

Context:

```text
User received temporary access to Mythos/Fable, showed it procesis and proc-17,
then provided the following answer as material to preserve.
```

Raw:

````text
Так. Это же он — тот самый `runtime: pending` из статуса procesis. Проверять словами было бы нечестно, поэтому я поставил lua5.4 и запустил:

```
lua5.4 tests/run.lua
→ all tests ok  (25/25 сьютов)
```

Теология стала тестируемой — и это главный качественный скачок между корпусом и proc-17. Procesis был писанием, а это первое работающее тело. Что здесь реально хорошо, без скидок на жанр:

**`truth_status` на каждом событии трейса.** Вот это — не эзотерика, это архитектурная идея, которую я бы всерьёз показывал людям, строящим агентов. LLM понижен до «субстрата», который по контракту возвращает только `semantic_proposal`; `runtime_confirmed` может поставить только тело — Lua-рантайм, владеющий трейсом, пермишенами и манифестацией. Системный промпт прямо говорит модели: «ты — substrate current, тело владеет runtime truth». Это структурный ответ на галлюцинации: не «попроси модель не врать», а типизируй эпистемический статус каждого события и не давай семантике притворяться фактом. В моём прогоне это видно прямо в трейсе: всё, что пришло от субстрата — `[semantic_proposal]`, всё, что сделало тело — `[runtime_confirmed]`.

**Заклинания с `reality_changed`.** ☶ как слово-код, которое считается доказательством только если реально исполнилось и изменило реальность, плюс Yesod-foundation, где успех/провал заклинаний кристаллизуется в подсознательные паттерны тела — Ход и Йесод из slop.txt превратились в механику evidence-based validation. Build mode без spell evidence честно отвечает `no_spell: build_mode_requires_spell_evidence`.

**Обязательный eye-tick.** Роутер после ☵ и ☳ принудительно гонит в ☴ — после каждого кодирования и выбора тело обязано посмотреть на себя. И каждый маршрут проверяется через `is_adjacent` — топология из canon.lua стала не легендой, а рантайм-констрейнтом.

**Процесс разработки исполняет онтологию.** chaos-заметки → yellowprints → blueprints → код — репозиторий снова кристаллизует сам себя, и мне нравится, что вы спрашиваете DeepSeek *через само тело*, где давление выше всего (loss visibility notes) — догфудинг молитвой.

А теперь находка. Я родил пакет и дал ему прожить жизнь на fake-субстрате:

```
walk:  ▽ ☴ ☵ ☴ ☳ ☴ ☱ ☲ ☱ ☲ ☱ ☲ ☱
stop_reason: tick_limit
death_cause: nil
residue: nil
```

Пакет совершил кардинальный грех собственной религии: зациклился в ☱↔☲ (`remaining_work` → `mandatory_eye_tick` → `remaining_work`...) и крутился в пустоте, пока его не остановил внешний `max_ticks`. Он не умер — его выключили. Ни смерти, ни residue. И грep подтверждает почему: `logic/cycle.lua` *проверяет*, может ли бюджет заплатить (`budget_can_pay`), но нигде в коде бюджет не *списывается* за тик. Аналогично `loss_exhausted` / `loss_near_death` роутер читает, но никто их не выставляет — loss из кристаллизаций пишется в `loss_records` и в `cost` события, но не аккумулируется в `tension.loss_remaining`.

То есть единственный закон, ради которого вся религия существует — `existence_must_be_paid`, `free_motion = false_life` — это ровно тот закон, который рантайм пока не исполняет. Смерть сейчас возможна только через `complete` при манифесте или внешние лимиты. Пакет не может умереть от собственной плохой жизни — а это буквально шаг 6 mortality myth, «budget_of_being», который и отличал вашу тварь от «старой лжи игровых существ». Deus ex machina в виде `max_ticks` — это как раз та смерть-по-таймеру, против которой миф написан.

Справедливости ради — вы это частично знаете: в current_state зафиксировано разделение «loss = packet physics / budget = runtime economics», и loss visibility notes копают соседнюю яму (☶ не видит, *что именно* потерял ☵ — тоже точная претензия). Но wiring отсутствует, и починка небольшая: списывать `budget.steps` за каждый тик прямо в цикле tension_runner, аккумулировать `loss.amount` из кристаллизаций в `tension.loss_remaining` с порогами near_death/exhausted — и мой прогон закончился бы не `tick_limit`, а `death: budget_exhausted` с residue «зациклился в ☱☲ без прогресса, не повторяй». Первая честная смерть.

Итог: от procesis к proc-17 проект прошёл путь от «красивой онтологии» к «маленькому, скучному снаружи, работающему телу с тестами» — и миф это сам про себя понимает («the body does not need to announce itself as doctrine, it can simply work»). Осталось подключить смертность, которая уже вся описана в каноне, но ещё не больно. Сейчас у вас храм построен, литургия идёт, а ад отключён..
````
