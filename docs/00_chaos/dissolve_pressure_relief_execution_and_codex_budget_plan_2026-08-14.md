# DISSOLVE Pressure-Relief Execution And Codex Budget Plan

```text
layer: CHAOS
date: 2026-08-14
status: active execution plan
physical baseline: de33611c0037
physical baseline published: yes, origin/main
runtime implementation after baseline: R8.1-R8.6 pure reader and runner hook
current technical gate: campaign publication complete
runtime authority change authorized here: no
TABLE amendment: R6 completed 2026-08-20 from recorded audit findings
CRYSTALL: R7 completed 2026-08-20 by explicit machinist instruction
CRYSTALL result:
  docs/02_crystall/blueprints/dissolve_pressure_relief.v0.md
R8 implementation authorized: yes, explicit machinist instruction 2026-08-20
model policy: R7 completed on High; R8 defaults to High, publication can use Medium
operational constraint: weekly Codex access is finite and also needed for host administration
```

This document is the hot execution carrier for the next DISSOLVE campaign. It
does not replace either the conserved campaign history or the exact pause
marker:

```text
docs/00_chaos/dissolve_campaign_conservation_and_resume_notes_2026-08-12.md
docs/00_chaos/dissolve_pressure_relief_pause_and_resume_notes_2026-08-14.md
docs/00_chaos/dissolve_pressure_relief_cross_table_audit_r1_r5_notes_2026-08-20.md
```

One external-architecture guard also applies without entering the hot
DISSOLVE corpus:

```text
docs/00_chaos/external_harness_compatibility_and_non_duplication_notes_2026-08-14.md
```

It records that proc-17 owns work physics while DeepSeek Harness or another
external host may supply commodity userland. It authorizes no integration in
R1-R10. Its only immediate effect is to prevent this campaign or an adjacent
convenience task from growing a duplicate generic harness.

Named readers:

```text
current or future Codex session, before resuming DISSOLVE work
machinist, before authorizing CRYSTALL or runtime implementation
commit reviewer, at each publication gate in Section 8
```

Read trigger:

```text
the machinist says to return to DISSOLVE or asks what the next DISSOLVE step is
```

## 0. Resume In One Screen

The direct inherited-form treatment is finished and published. A real
QA-rejected lineage now grows the exact life:

```text
ancestor rejection
-> corpse and recovery carrier
-> NETWORK projection
-> FLOW materializes inherited rejected form
-> qualified pressure selects ▽ -> ☷
-> ☷ atomically releases that form into bounded residue
-> no irreversible identity loss
-> the living Packet continues ☷ -> ☴
```

The immediate aftermath has also been measured:

```text
selected release obligation: 1 -> 0
successor upper-OBSERVE obligation: 0 -> 1
aggregate qualified witness count: 1 -> 1
```

Therefore DISSOLVE pressure relief is not a fall in total witness count. The
supported v0 claim is narrower:

```text
the exact causal obligation that selected the exact release was discharged
```

The first experiment also exposed and corrected a topology false green:

```text
arrival at ☷ hides a witness targeting ☷ even when no release executes
```

The future reader must therefore compare the changed body at the same source
coordinate and report the real successor coordinate separately.

Current state:

```text
CHAOS measurement: complete
corrected executable experiment: green
R1-R5 cross-table audit: complete
R6 TABLE amendment: complete
TABLE reader contract: crystallized
CRYSTALL: R7 complete
production reader: absent
general semantic GC: absent and out of scope
next exact action: explicit machinist authorization for R8 implementation
```

## 1. Why This Plan Exists

Codex Ultra currently carries the whole proc-17 architecture accurately, but
large cold ingestions consume a material part of the machinist's weekly Codex
allowance. Reading the DeepSeek Harness repository and its 88-page Cordis
paper consumed approximately four percent of that allowance. Exhausting the
allowance would remove not only an engineering collaborator but the Codex
session used to administer this computer.

The selected policy is not to lower the reasoning level. It is:

```text
preserve Ultra reasoning
reduce repeated ingestion
bound each campaign
publish exact checkpoints
use the documentation stack as cold corpus
carry only the current causal slice hot
```

This is a host-operation constraint, not Packet physics. It must not be used
as evidence for runtime budget, loss, pressure, mortality or model quality.
The machinist supplied the cost observation; this document records it as an
operational fact and derives a work protocol from it.

## 2. Mission Of This Campaign

Finish one bounded treatment-specific reader:

```text
dissolve.inherited_rejected_form_release.v0
-> dissolve.pressure_relief.v0
```

The reader must prove, without mutating the Packet, that:

```text
one exact selected qualified witness
joined one exact committed action and route
joined one exact destination tick and release effect
joined the exact resulting target and residue state
became absent under a same-coordinate post-effect derivation
while real successor obligations remained visible and separate
```

The campaign ends after the pure reader, hostile controls, documentation and
publication gates are green. It does not continue automatically into generic
raw-relation collection, formed-relation collection, ageing, mark/sweep,
full-tree promotion or DeepSeek Harness integration.

## 3. Current Evidence Boundary

Runtime-confirmed:

```text
the grown inherited-form release life and its no-rigidity control
exact qualified witness and action selection
committed E02 ▽ -> ☷ and executed destination tick
atomic live -> dissolved target transition
bounded rejected-form residue
released_mass = {forms=1, relations=0}
irreversible_identity_loss = 0
actual E07 ☷ -> ☴ successor
same-coordinate false-green control
selected obligation absent after the real effect
aggregate count unchanged at 1 -> 1
```

Document decisions only:

```text
the production reader input schema
the complete source-identity join
the same-obligation key
not_measurable / not_discharged / discharged classifications
the detached dissolve.pressure_relief.v0 result schema
the writer-reader ledger and hostile controls
```

Not authorized:

```text
a scalar or universal vector Z formula
relief written by the DISSOLVE event itself
route arrival used as effect evidence
a second mutable pressure ledger
multiple merged release actions in v0
general semantic-GC authority
```

## 4. Bounded Execution Sequence

The sequence remains R1-R10. R1-R5 form one audit campaign and should reuse
one ingestion rather than restart from the repository root five times.

| Step | Work | Required output | Runtime code |
|---|---|---|---|
| R1 | Audit selected qualified pressure and action composition | Every selected identity required by the reader has one authoritative writer and path | forbidden |
| R2 | Audit route, arrival and trace ordering | Candidate, committed and executed evidence cannot be confused; topology-only disappearance remains visible | forbidden |
| R3 | Audit atomic DISSOLVE effect and final-state joins | Release event, target version, residue and readiness can be joined exactly | forbidden |
| R4 | Audit camera and post-effect snapshot ordering | Same-coordinate derivation is pure; real post snapshot is provably later than the effect | forbidden |
| R5 | Audit upper-OBSERVE successor classification | New obligation is retained without being charged against completed release | forbidden |
| R6 | Record findings and amend TABLE | Every contradiction or underspecification receives an explicit disposition | forbidden |
| R7 | Write exact CRYSTALL | One implementable pure-reader contract, no invented policy | forbidden |
| R8 | Implement one pure reader | Detached deterministic value; no trace, cache, watermark, substrate or authority mutation | allowed by R7 only |
| R9 | Grow focused and hostile controls | PR-T01 through PR-T20 covered or explicitly blocked before implementation proceeds | tests only beyond R8 |
| R10 | Run publication gates and update current state | Focused, static, full, mortality and diff gates green; exact claims published | no new behavior |

Status amendment 2026-08-20:

```text
R1-R5 complete in one bounded audit
R6 complete; TABLE amended
R7 complete; exact CRYSTALL written
R8.1 complete; strict schemas and focused schema tests green
R8.2 complete; grown body route/snapshot/witness/action join green
R8.3 complete; destination tick, exact release, target/residue final state,
runtime frame, optional v3 arrival and old-action finality join green
R8.4 complete; canonical version-independent obligation identity and pure
same-coordinate `▽` control green
R8.5 complete; stored actual `☷` successor classified and deterministic
`dissolve.pressure_relief.v0` measured/not-measurable views green
R8.6 complete; explicit runner option, exact post-`router.after_tick` capture,
transient v3 corroboration, outer Packet purity guard and literal off/v0
masslessness ablation green
R9 complete; PR-T01 through PR-T20 have explicit owners across focused,
hostile and runner suites; all four narrow suites and static parse are green
R10 complete; 140-suite full run, mortality 8/8, all-Lua static parse and diff
gates green; conserved CHAOS and current-state claims updated for publication
```

### R1-R5 audit questions

The audit must answer these questions from code and current contracts, not by
inventing a convenient payload:

```text
Who writes each source identity?
Who is allowed to read it?
Can the reader resolve it from refs alone?
Can candidate route evidence masquerade as committed or executed evidence?
Can arrival at ☷ hide the old witness without an effect?
Can an effect event disagree with current target or residue state?
Can a stale action remain executable after release?
Can the camera place the post snapshot before the effect?
Can the same-coordinate control accidentally acquire route authority?
Can successor OBSERVE pressure be mistaken for unresolved release pressure?
Can any missing source be laundered into zero relief?
```

Any real contradiction returns the work to R6. It must not be patched directly
in runtime code.

## 5. Cold-Ingestion Protocol

The next Codex session must not begin by reading the full repository. Load the
smallest sufficient chain in this order.

### Tier A: mandatory hot carrier

```text
1. this execution plan
2. dissolve_pressure_relief_pause_and_resume_notes_2026-08-14.md
3. dissolve_pressure_relief_reader_yellowprint.v0.md
4. the DISSOLVE roadmap lines in docs/03_manifest/current_state.md
```

### Tier B: R1-R5 authority sources

Read only the sections needed from:

```text
qualified_dissolve_inherited_form_yellowprint.v0.md
pressure_need_and_action_composition_yellowprint.v0.md
operator_tree_physics_yellowprint.v0.md
the exact route/edge evidence contract
the exact runtime camera contract
the exact upper-observation contract
the exact atomic release implementation and focused tests
```

Resolve filenames with `rg`; do not infer them from memory.

### Tier C: escalation only

Read older CHAOS archaeology, the full project audit, unrelated QA campaigns,
UPM source documents or external papers only if Tier A/B exposes a concrete
contradiction that cannot be resolved locally. Record the reason before the
extra ingestion.

During R1-R10:

```text
no general full-repository reread
no external web or paper branch
no DeepSeek Harness integration work
no repetition of already green full gates without a changed relevant surface
```

An unexpected idea receives one bounded residue note or waits until the
current campaign checkpoint. It does not redirect the active campaign.

## 6. Verification Economy

Verification remains strict. Economy comes from matching each gate to the
changed surface, not from skipping evidence.

Documentation-only R1-R7:

```text
targeted source/code cross-reference
schema and writer-reader audit
git diff --check
no full Lua suite merely because prose changed
```

Implementation R8-R9:

```text
luac -p on each changed Lua file
focused pressure-relief reader test after each coherent code slice
hostile topology/identity/order/masslessness controls at R9
no external model call; the reader is deterministic
```

Publication R10, once per final code state:

```text
focused reader suite
full lua tests/run.lua
mortality battery
git diff --check
working-tree scope audit
```

A passing full suite is not repeated merely because a commit or push follows.
Repeat it only when the tested code state changes or the prior run is no longer
the state being published.

## 7. Access-Reserve And Interruption Law

The machinist reports the remaining weekly allowance when it matters. This
document sets no fabricated numeric threshold. The operational law is:

```text
do not deliberately spend Codex access to zero
```

When the machinist declares the reserve low:

```text
finish only the current atomic edit or audit finding
run the narrow gate appropriate to that edit
write an exact pause marker if the current one is no longer accurate
report dirty files and the next exact command/step
do not open a new architectural branch
```

If a tool or product limit interrupts work, the next session reads Tier A and
continues from the recorded step. It does not reconstruct the campaign from
chat history.

This rule protects access to Codex as the current host-administration tool. It
does not grant Codex runtime authority inside proc-17 and does not make model
availability part of Packet truth.

## 8. Publication Checkpoints

Three checkpoints bound the remaining work.

### Checkpoint A: audited documents

```text
R1-R6 complete
TABLE amended with every finding
audit disposition recorded
runtime unchanged
status: satisfied 2026-08-20
```

### Checkpoint B: implementation authority

```text
R7 complete
CRYSTALL names exact module, schema, failures and controls
machinist authorizes code: pending
```

### Checkpoint C: finished treatment reader

```text
R8-R10 complete
reader and tests green
current_state and MANIFEST claims exact
commit pushed
general semantic GC still explicitly pending
```

At each checkpoint, commit scope must distinguish:

```text
runtime-confirmed evidence
document_decision
diagnostic-only output
deferred hypothesis
```

## 9. Forbidden Shortcuts

```text
do not let ☷ certify its own relief
do not compare different topology coordinates as a causal before/after pair
do not count route arrival as execution
do not treat missing evidence as zero
do not use aggregate witness delta as the selected-obligation result
do not persist a second mutable truth store
do not let the reader append Packet trace or charge budget/loss
do not patch runtime before TABLE audit and CRYSTALL
do not broaden v0 to multiple actions, raw relations or formed relations
do not promote the router from one treatment-specific green life
do not expand a reference CLI, provider or store into generic harness
functionality without applying the external-harness build-or-reuse gate
```

## 10. Completion Definition

This campaign is complete only when all are true:

```text
R1-R6 cross-table audit closed
R7 exact CRYSTALL accepted
one pure dissolve.pressure_relief.v0 reader implemented
the arrival-only false green is red for relief
the exact release life is discharged_with_successor_obligation
no-rigidity receives no selected treatment or relief credit
trusted contradictions fail loudly as world/harness defects
reader ablation leaves route, state, budget, loss, corpse and edge credit equal
focused, full, mortality and static gates pass on the published state
current_state names what is complete and what remains deferred
commit is pushed
```

The next campaign after completion is a new decision. The current evidence
does not automatically authorize semantic age, every-tick collection,
raw/formed relation GC, a universal Z model, full-tree promotion or a Cordis
integration.

## 11. Next Exact Action

```text
No next implementation action is authorized by this campaign. Commit and push
this complete R1-R10 publication unit. Any work on semantic age, every-tick
collection, generic relation GC, raw/formed relation promotion, a universal Z
model or external-harness integration begins as a new CHAOS decision rather
than an amendment smuggled through this reader.
```
