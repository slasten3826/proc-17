# DISSOLVE R3 Raw-Stale Growth Red Observation

```text
layer: CHAOS / runtime diagnostic
date: 2026-08-12
campaign step: R3
checkpoint revision: d581f63
body implementation changed: no
router authority changed: no
TABLE or CRYSTALL amendment authorized: no
main-suite registration: intentionally absent
result: RED at the body-fact writer boundary
```

## 0. Result

The proposed first DISSOLVE treatment cannot yet be grown honestly.

The local field already derives an exact raw relation as `stale` when an
endpoint version changes. The raw DISSOLVE organ already accepts that phase
without a fixture release policy. Those two facts do not form a living route,
because the current ordinary body has no sequence that changes an endpoint
between CONNECT recognition and ENCODE consumption.

```text
☰ CONNECT writes current raw identity
-> qualified relation formation immediately commits ☵
-> ☵ ENCODE terminally consumes that raw identity
-> no ordinary endpoint-mutating operator occurs in between
-> no stale raw fact is born
-> no qualified DISSOLVE life can be measured
```

This is a red diagnostic, not evidence that DISSOLVE should be forced into the
route.

## 1. Command

```sh
lua tests/pending_dissolve_live_route_gate.lua
```

The pending gate remains outside `tests/run.lua` until its positive contract is
grown and the production treatment is accepted.

## 2. Observed Control Life

One ordinary Tree-authority vertical pair, with no substrate and no harness
route, grows:

```text
first destination tick:          ☰
raw phase after ☰:               available
committed outgoing edge:         ☰ -> ☵
next destination tick:           ☵
raw phase after ☵:               encoded
DISSOLVE destination tick:       absent
endpoint-mutating CHOOSE tick:   absent
```

The immediate relation consumer behaves consistently with its accepted
contract. Delaying ENCODE merely to make DISSOLVE easier to test would be
storage theater unless another real body need owns that delay.

## 3. Attempted Body Mutation

A second ordinary life adds a strict two-alternative semantic proposal. This
was the smallest existing path capable of eventually reaching a real CHOOSE
and changing exact unit versions.

It does not produce the desired pair. After semantic OBSERVE, two independent
causal affordances coexist:

```text
☰ relation recognition over the vertical pair
☵ strict structure formation over the observed proposal
```

The qualified policy reports `ambiguous_pressure`. It does not execute either
action, does not create a raw relation, and never reaches CHOOSE. Enabling a
canonical control fallback would contaminate the experiment and still would
not prove a varying DISSOLVE witness.

This result is useful because it prevents a false claim:

```text
the body has a CHOOSE operator
therefore it can naturally stale any prior relation endpoint
```

The premise is true. The conclusion is false under current object domains and
route choreography.

## 4. Two Separate Missing Boundaries

R2 and R3 now expose two independent gaps.

### 4.1 Missing fact writer/choreography

No ordinary current path produces an exact stale raw relation before ENCODE
terminally consumes it. Existing stale tests use harness transitions and a
fixture activation mutation. They prove the pure phase function, not a living
Packet cause.

### 4.2 Missing qualified reader/action

Even if a stale state is prepared locally:

```text
runtime/qualified_pressure.lua has no DISSOLVE witness producer
runtime/pressure_action.lua has no DISSOLVE action mode
```

Therefore adding only a stale-state producer would still leave an unread
record. Adding only a DISSOLVE reader would leave a reader whose production
fact never occurs.

## 5. What This Red Result Rejects

```text
do not inject options.reason from the harness
do not call organs/dissolve directly and count the call as P10
do not mutate an endpoint in on_packet_birth or a test callback
do not suppress ENCODE merely to preserve garbage for DISSOLVE
do not use canonical fallback as the missing causal law
do not call the vertical fixture relation production promotion evidence
do not implement semantic age to hide the missing current-law writer
```

## 6. Candidate Directions After R3

The experiment does not yet choose among these directions:

### A. Direct body-owned form target

Use an exact form that already becomes obsolete through ordinary body work,
such as a CHOOSE-suppressed structured alternative. This aligns with the
existing direct-unit direction in the blocked-lineage TABLE amendment, but the
unit-target DISSOLVE contract is not crystallized or implemented.

### B. Inherited failed form

Complete the already tabled chain:

```text
blocked ancestor -> repair bequest -> FLOW materialized failed-form unit
-> qualified rigidity witness -> direct-unit DISSOLVE
```

This is the intended P03/P10 repair path, but current runtime still lacks most
of that chain and it is larger than a first local treatment.

### C. Production relation lifecycle with a real mutable endpoint

Define a body-native relation predicate over objects that an existing organ
later changes, then prove the exact before/after relation lifecycle. This may
be the most complete relation-machine result, but it requires a new causal
consumer and must not be invented solely to close P10.

### D. Current raw unsupported/replaced fact

Look for an ordinary writer of a later raw epoch or an unsupported relation
fact. Current inspection found the same dependency cycle as stale: a relevant
object/domain change must occur before CONNECT can lawfully re-arm.

## 7. Decision Boundary

R4-R7 cannot honestly run the originally proposed matched positive life yet.
The next legitimate step is a document decision selecting the smallest
existing body fact, then TABLE/CRYSTALL amendment if that selection changes the
accepted action schema.

The leading candidate after this red result is a direct unit/form target rather
than a synthetic active relation. It has three advantages:

```text
the body already creates and versions exact units
CHOOSE already suppresses alternatives through a real operator tick
DISSOLVE can release obsolete form without inventing a relation handle
```

This is still a candidate. The red gate has earned the question; it has not
answered it.

Subsequent current-code audit rejected both same-life cleanup shortcuts as the
first treatment and selected the NETWORK rejected-generation direction. The
newer synthesis is recorded in:

```text
docs/00_chaos/dissolve_network_rejected_generation_target_notes_2026-08-12.md
```

That document also resolves the old grave-repair archaeology against the newer
law that QA rejection belongs to lineage, corpse and carrier rather than grave
classification.

## 8. Truth Status

```text
ordinary current relation is immediately encoded: runtime_confirmed
alternative mutation attempt ends ambiguous: runtime_confirmed
no current qualified DISSOLVE producer/action: runtime_confirmed code audit
direct-unit target is the next best treatment: document_hypothesis
semantic age is required now: rejected by current evidence
```
