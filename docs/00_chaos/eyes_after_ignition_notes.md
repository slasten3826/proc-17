# Eyes After Ignition Notes

Raw conclusion after first live DeepSeek ignition.

The body has a spark now, but it does not yet have eyes.

## What Happened

DeepSeek was called inside `proc-17` packet life.

The body worked:

```text
packet birth
mode_enter
substrate_call
substrate_result as semantic_proposal
tool_call/tool_result
manifest
death
trace
```

But the substrate did not receive real repository evidence.

So it built `proc-17` from word-attractors:

```text
proc      -> process table / scheduler / stack / heap
packet    -> TCP / UDP / protocol maps
body      -> organs / biology analogies
substrate -> semiconductor current
```

That is not runtime truth.

It is unsupported semantic form.

## Eye Split

The body has two centers:

```text
☴ OBSERVE
☱ RUNTIME
```

These are two different eyes / seeing functions.

`OBSERVE` sees toward chaos:

```text
files
repo tree
raw outputs
substrate text
tool evidence
unsupported forms
```

`RUNTIME` sees toward manifest:

```text
budget
permissions
continuation
death
residue
what can actually be sustained
```

The second eye is farther away.

The first eye is needed now.

## First Eye: Repo Context Organ

Current need:

```text
repo_context_organ
```

Purpose:

```text
give substrate real repo evidence before asking it to reason
```

Minimum shape:

```text
list repo files
select relevant files
read selected files
produce compact context payload
send context payload to substrate
mark repo evidence as runtime_confirmed
keep substrate interpretation as semantic_proposal
```

This is an OBSERVE-side organ.

It should be read-only at first.

## Why Read-Only First

The sandbox v0 is already enough for read-only repo context:

```text
relative paths only
no absolute paths
no parent traversal
read allowed
shell denied
write guarded by mode
```

So the first eye can open without giving the substrate hands.

## Consequence

Before more meaningful DeepSeek tests:

```text
no more "understand repo" prompts without repo evidence
```

The body should first:

```text
OBSERVE repo evidence
then call substrate
then compare proposal against evidence
```

This turns hallucination from uncontrolled drift into measurable mismatch.

## Later Eye: Runtime

The runtime eye is not first.

It will eventually see:

```text
which traces can be resumed
which residues matter
which loops are dying
which costs are sustainable
which claims survived execution
```

But building that now would likely create fake memory.

So current order:

```text
1. OBSERVE eye: repo context
2. then deeper DeepSeek ignition
3. then RUNTIME eye / residue memory
```

